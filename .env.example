# Optional — defaults to http://localhost:4000/api (see vite.config.ts)
# VITE_API_BASE_URL=http://localhost:4000/api

# Invoice intake service on Steve (OCR + LLM drafts) — Steve npm run dev
# VITE_INVOICE_INTAKE_URL=http://localhost:4101
# VITE_INVOICE_INTAKE_API_KEY=change-me-dev-key

# Dell AppImage build: copy .env.production.example → .env.production
# (loaded by vite build mode=production; see docs/invoice-intake-prod-pilot.md)
